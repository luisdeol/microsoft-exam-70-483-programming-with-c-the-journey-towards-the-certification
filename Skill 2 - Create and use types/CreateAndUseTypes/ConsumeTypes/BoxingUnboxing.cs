namespace ConsumeTypes
{
    public class BoxingUnboxing
    {
        //static void Main(string[] args)
        //{
        //    var arrayList = new ArrayList { 4, true, "a string" }; // these 3 values are boxed, since the ArrayList receives a list of object values are parameter

        //    foreach (var value in arrayList)
        //        Console.WriteLine(value.ToString()); // values are unboxed and displayed in the screen

        //    Console.ReadKey();
        //}
    }
}
